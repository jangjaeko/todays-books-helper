# CLAUDE.md

이 파일은 Claude Code(및 다른 AI 도구)가 이 저장소에서 작업할 때 참고하는 프로젝트 컨텍스트입니다.

## 프로젝트 개요

YES24 도서 상세페이지에서 필요한 서지정보만 추출해서, **엑셀에 바로 붙여넣을 수 있는 형태(탭 구분 텍스트)** 로 클립보드에 복사해주는 개인용 Chrome 확장 프로그램입니다.

- 용도: 개인용 / 비상업적
- Chrome Manifest V3, 빌드 과정 없는 순수 정적 파일 (번들러·프레임워크 없음)
- 웹스토어 미배포. `chrome://extensions` → 개발자 모드 → "압축해제된 확장 프로그램 로드"로 사용

## 파일 구조

```
yes24-extractor/
├── manifest.json   # MV3 매니페스트 (permissions: activeTab, scripting, storage)
├── popup.html      # 팝업 UI (할인율 입력 + 케이스별 복사 버튼 + 원본값 보기)
├── popup.js        # 추출 로직 + 캐나다가격 계산 + 케이스별 컬럼 조립
└── CLAUDE.md
```

`popup.js`는 크게 두 부분:

1. `extractYes24BookInfo()` — `chrome.scripting.executeScript`로 **YES24 페이지 안에 주입되어 실행**됨.
   - **중요**: 주입 함수라 외부 스코프의 변수·함수를 참조할 수 없음. 필요한 헬퍼는 함수 내부에 전부 정의해야 함.
2. 팝업 UI 로직 — 추출 결과 렌더링, 할인율 관리, 케이스별 복사.

동작 범위: `https://www.yes24.com/product/goods/` 로 시작하는 URL에서만 동작.

## 추출 필드와 규칙

| 필드 | 키 | 규칙 |
|---|---|---|
| ISBN | `isbn` | 품목정보 표의 **ISBN13** 값 |
| 제목 | `title` | `<title>` 또는 `og:title`을 `\|`로 나눈 첫 조각 (`- 예스24` 제거) |
| 저자 | `author` | **YES24에 표기된 문자열 그대로** (역할 표기 포함) |
| 출판사 | `publisher` | 제목 아래 정보줄의 출판사 |
| 출판일자 | `pubDate` | **YYYYMM** 형식 (예: `202607`) |
| 무게 | `weight` | **숫자만, 단위 g 생략** (예: `188`) |
| 정가 | `priceKRW` | 상품 가격정보 표의 **정가** (판매가/할인가 아님), 숫자만 |
| Subject | `subject` | "관련분류 > 카테고리 분류"의 **첫 번째 경로만**, `>` 로 이어붙임 |

### 저자 표기 관련 (중요)
`<title>`에는 대표 저자만 나오므로 **`<title>`을 저자 추출에 쓰면 안 됨**. 반드시 본문 정보줄에서 가져와야 함.

- 단순 케이스: `이애월 저`
- 복합 케이스: `흔한남매 원저/안치현 글/유난희 그림/흔한컴퍼니, 이정모 감수`
  (이 경우 `<title>`에는 "흔한남매"까지만 나옴)

"저", "지음", "옮김" 같은 접미어를 **제거하지 않고 그대로** 유지해야 함.

### 저자/출판사/출판일 추출 방식 (이중 안전장치)
`a[href*="authorNo="]` 링크를 기준점으로 정보줄 컨테이너를 찾은 뒤:

- **(a) 앵커 위치 기반 (우선)**: 마지막 저자 링크 다음에 나오는, `authorNo=`가 아닌 첫 링크를 출판사로 간주.
  - 출판사 링크는 `javascript:void(0);` 인 경우가 많아 href 패턴으로 못 찾음 → 위치로 판단.
  - "리뷰", "보기", "알림", 숫자만 있는 텍스트는 제외.
- **(b) `|` 텍스트 분리 (보조/폴백)**: `textContent`를 `|`로 나눠 `[저자, 출판사, 날짜]`로 파싱.
  - 페이지에 따라 구분자 `|`가 실제 텍스트가 아니라 CSS로만 그려질 수 있어 (a)를 우선함.

표(`table tr`)의 `th`/`td` 라벨 매칭은 발행일·무게·ISBN·정가에 사용. 못 찾으면 `document.body.innerText` 정규식 폴백.

## 캐나다 가격 계산

```
x   = (KRW × 할인율 / 960) + (무게(g) × 0.001 × 13)
raw = x + (x × 53 / 47)          // = x × 100/47
가격 = Math.ceil(raw / 0.5) × 0.5  // 0.5 단위 올림
```

- **할인율**: 기본 `0.775`, 팝업 UI에서 수정 가능. `chrome.storage.local`에 저장되어 다음 실행에도 유지됨.
- **0.5 단위 올림 규칙**: `10 < raw ≤ 10.5` → `10.5` / `10.5 < raw ≤ 11` → `11`
- **무게 정보가 없으면 계산하지 않고 `0`으로 처리** (KRW가 없을 때도 동일)
- 부동소수점 오차 때문에 `Math.round(raw * 1e6) / 1e6` 으로 보정 후 올림.

검증 예시:

| 책 | KRW | 무게 | raw | 결과 |
|---|---|---|---|---|
| 사라지는 돈… | 21000 | 502 | 49.956 | 50 |
| 흔한남매 15 | 16800 | 450 | 41.303 | 41.5 |

## 복사 양식 (3가지 케이스)

**모든 출력은 탭(`\t`) 구분** — 엑셀에 붙여넣으면 셀 단위로 나뉘어야 함. 라벨("제목:", "저자:" 등)은 **붙이지 않고 값만** 출력.
아래 표에서 `|` 하나 = 엑셀 셀 하나. "빈칸"은 빈 문자열, "Copies"는 값을 채우지 않음(사용자가 직접 입력).

### Case 1 — SALES
```
캐나다가격 | 빈칸 | Title | Publisher | Author | Copies | KRW | Weight
```

### Case 2 — 선박
```
ISBN | 제목 | 캐나다가격 | 빈칸 | 빈칸 | Copies | 빈칸 | Author | 출판일자 | Publisher | Subject | Copies | KRW | Weight
```

### Case 3 — LBI (책 1권당 **2줄** 사용)
1번줄과 2번줄을 `\n`으로 이어 붙여 복사. 12개 컬럼.

| # | 1번줄 | 2번줄 |
|---|---|---|
| 1 | ISBN | (빈칸) |
| 2 | (빈칸) | Title |
| 3 | 캐나다가격 | (빈칸) |
| 4 | 빈칸 | 빈칸 |
| 5 | 빈칸 | 빈칸 |
| 6 | Copies | Copies |
| 7 | 빈칸 | 빈칸 |
| 8 | (빈칸) | Author |
| 9 | Pub.Date | (빈칸) |
| 10 | 빈칸 | 빈칸 |
| 11 | (빈칸) | Publisher |
| 12 | Subject | (빈칸) |

## 현재 상태 / 미해결 이슈

- **Case 2(선박) 컬럼 밀림 버그 (미해결)**
  - 증상: 붙여넣었을 때 Publisher(출판사) 칸이 비면서 이후 값들이 한 칸씩 밀린다는 보고.
  - 재현 URL: `https://www.yes24.com/product/goods/133973965` (아무튼, 라디오 / 이애월 저 / 제철소)
  - 참고: 같은 페이지에서 Case 1, Case 3은 정상 동작한다는 보고가 있음 → 추출 실패보다는 `buildCase2()`의 컬럼 배열 문제일 가능성이 큼. 다만 사용자 보고가 엇갈린 부분이 있어 **재현 확인 먼저 필요**.
  - 디버깅 시 확인할 것: 팝업의 "추출된 원본 값 보기"에서 `publisher`가 실제로 채워지는지 → 채워진다면 `buildCase2()`의 배열 길이/순서를 위 표와 대조.

- **Subject 추출 취약성**: "관련분류" 텍스트를 가진 요소를 찾아 상위로 올라가며 카테고리 링크를 수집하는 방식이라, 페이지 구조가 다른 상품에서 실패할 수 있음. 여러 상품으로 테스트 필요.

## 작업 시 주의사항

- YES24는 페이지 유형(도서/외서/중고/eBook 등)마다 DOM 구조가 조금씩 다름. 셀렉터를 하나만 믿지 말고 **폴백을 함께 둘 것**.
- 클래스명 기반 셀렉터는 사이트 개편에 취약함. 라벨 텍스트(`발행일`, `ISBN13`, `정가`) 매칭이나 링크 href 패턴처럼 **의미 기반**으로 찾는 쪽을 선호.
- 수정 후에는 `chrome://extensions`에서 새로고침(⟳) 버튼만 누르면 반영됨.
- 새 케이스나 컬럼 순서를 추가할 때는 이 문서의 "복사 양식" 표도 함께 업데이트할 것.
